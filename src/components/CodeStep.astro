---
interface Props {
  command: string
  status?: 'running' | 'done' | 'warning'
}

const { command, status } = Astro.props
---

<pre data-prefix={status ? ">" : "$"} class:list={[status === 'warning' && 'text-warning', status === 'done' && 'text-success']}><code>{command}</code></pre>
